{
  "name": "asas-challenge",
  "version": "0.1.0",
  "private": true,
  "description": "ASAS Challenge - real-time 30-minute employee engagement event (Elm identity, built using ASAS)",
  "workspaces": [
    "packages/shared",
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "build": "npm run build -w packages/shared && npm run build -w apps/api && npm run build -w apps/web",
    "build:shared": "npm run build -w packages/shared",
    "test": "npm run test -w packages/shared && npm run test -w apps/api",
    "dev:api": "npm run start:dev -w apps/api",
    "dev:web": "npm run start -w apps/web",
    "db:up": "docker compose up -d postgres",
    "lint": "npm run lint -w apps/api"
  },
  "engines": {
    "node": ">=20"
  }
}
